
```
docker run -d \
  --network host \
  --name firefox \
  jlesage/firefox
```

```
rclone copy /home/music gdrive:/home/music --progress
```

```
docker run -d \
  --name firefox \
  --network host \
  linuxserver/firefox
```
